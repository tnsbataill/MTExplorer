"""Startup functions for App"""
import os
import utils.strings

def startup() -> None:
    """Initialization and checks for app"""
    #if not os.path.exists(os.path.expanduser("~") + utils.strings.BOMS_PKL_PATH):
        #os.makedirs(os.path.expanduser("~") + utils.strings.BOMS_PKL_PATH)
    pass
